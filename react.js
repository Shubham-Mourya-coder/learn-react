
const heading = React.createElement("h1",{ id:"heading1"},"Hello World from Reacr!");

const root = ReactDOM.createRoot(document.getElementById("root"));

console.log(heading);

root.render(heading);
   